// Get the first two letters of the user name.
// function getInitials(name) {
//     if (name) {
//         const parts = name.split(' ');
//         const firstNameInitial = parts.length > 0 ? parts[0][0] : '';
//         const lastNameInitial = parts.length > 1 ? parts[parts.length - 1][0] : '';
//         return `${firstNameInitial}${lastNameInitial}`.toUpperCase();
//     }
//     return '';
// }
// const adminInitials = userInfo ? getInitials(userInfo.fullname) : null;
